{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "fullname" . }}
  labels:
    app: {{ template "name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  {{- if or .Values.global.hosts.externalIP .Values.service.loadBalancerIP }}
  loadBalancerIP: {{ coalesce .Values.service.loadBalancerIP .Values.global.hosts.externalIP }}
  {{- end }}
  externalTrafficPolicy: "Local"
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    - port: 443
      targetPort: 443
      protocol: TCP
      name: https
    - port: 22
      targetPort: 22
      protocol: TCP
      name: ssh
  selector:
    app: {{ template "name" . }}
    release: {{ .Release.Name }}
{{- end -}}

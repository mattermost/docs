{{- if .Values.enabled -}}
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ template "fullname" . }}
  labels:
    app: {{ template "fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  replicas: {{ .Values.replicas }}
  template:
    metadata:
      labels:
        app: {{ template "fullname" . }}
      annotations:
        checksum/configmap: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
    spec:
      securityContext:
        runAsUser: 1000
        fsGroup: 1000
      initContainers:
        - name: configure
          command: ['sh', '/config/configure']
          image: busybox
          volumeMounts:
          - name: config-volume
            mountPath: /config
            readOnly: true
          - name: init-{{ .Release.Name }}-secret
            mountPath: /init-config/registry
            readOnly: true
          - name: {{ .Release.Name }}-secret
            mountPath: /registry
            readOnly: false
      containers:
        - name: registry
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ default "" .Values.image.pullPolicy | quote }}
          volumeMounts:
          - name: {{ .Release.Name }}-secret
            mountPath: '/etc/registry-secret'
            readOnly: true
          - name: config-volume
            mountPath: /etc/docker/registry/
            readOnly: true
          resources:
{{ toYaml .Values.resources | indent 12 }}
      volumes:
      - name: init-{{ .Release.Name }}-secret
        secret:
            secretName: {{ .Values.certificate.secret }}
            items:
                - key: {{ .Values.certificate.key }}
                  path: {{ .Values.certificate.key }}
            defaultMode: 0400
      - name: {{ .Release.Name }}-secret
        emptyDir:
          medium: "Memory"
      - name: config-volume
        configMap:
          name: {{ template "fullname" . }}
{{- end -}}

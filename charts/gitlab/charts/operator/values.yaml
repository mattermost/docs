# Default values for sidekiq.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
image:
  repository: registry.gitlab.com/charts/components/gitlab-operator@sha256
  tag: c8cd1f2ab0bb837d33abf131c88486d07c9b773a807de1bfb099adbb96658412
  pullSecrets: []
  # pullPolicy: IfNotPresent

# Which version of the operator to deploy
version: 0.1

init:
  resources:
    requests:
      cpu: 50m

install:
  # Install the operator as well
  enabled: true
  image:
    repository: gcr.io/google_containers/hyperkube
    tag: v1.9.0
    pullSecrets: []
    #pullPolicy: IfNotPresent

resources:
  # limits:
  #  cpu: 1
  #  memory: 2G
  requests:
    cpu: 100m
    memory: 64M
